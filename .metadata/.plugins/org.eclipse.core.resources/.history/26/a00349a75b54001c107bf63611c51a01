#include  "speed.h"
#include "headfile.h"
/***********速度各变量声明**************/
int16 aim_speed=0 ;         //目标速度
int16 real_speed=0;        //实时速度
int16 last_speed;        //上一次速度
int32 distance;          //车子行走的路程
int16 Speed_PWM=0;         //速度环PWM
float circle_in=10;
float circle_out=8.3;
float deb_delt_p=0;
float deb_delt_i=0;
int32  angle;//以PWM差的形式表示打角，>0右转，小于0左转
uint16 S3010_Duty;//S3010的PWM
int16  a;//由Angle算出来的实际打角
float   Temp_Orr;
int32 Left_Speed;//中间变量
int32 Right_Speed;//中间变量
int16 All_PWM=0;     //总的PWM
int32   Left_High_Speed=0;//左电机PID设定值
int32   Right_High_Speed=0;//右电机PID设定值
#define sp_min -10000
#define sp_max 10000
#define hdsp_min -10000
#define hdsp_max 1000
//#define High_Speed 180

/*****************编码器初始化*****************
函数：void encoder_init(void)
功能：编码器初始化
参数：  无
说明： 编码器使用TIM1和TIM8，如更改引脚只需修改宏定义即可
    //第一个参数 表示选择使用的定时器
    //第二个参数 表示选择的计数引脚    计数引脚与方向引脚不可交换
    //第三个参数 表示选择的方向引脚
    gpt12_init(GPT12_T2, GPT12_T2INB_P33_7, GPT12_T2EUDB_P33_6);
返回值：
日期：2021-1-13    00:15
修改时间：2021-1-13    00:15
作者：  混子    */
//void encoder_init(void)
//{
//    gpt12_init(GPT12_T2, Ecoder_Pin1, Ecoder_Pin2);//初始化编码器
//}
/*****************速度测量*****************
函数：void speed_measure(void)
功能：测量实时速度，两个轮的平均速度 编码器采集值，
参数：  无
说明：  该函数可以测出速度
   speed = gpt12_get(GPT12_T2);
           gpt12_clear(GPT12_T2);
返回值：
日期：2021-1-13    00:15
修改时间：2021-1-13    00:15
作者： 混子    */

void speed_measure(void)
{ 
////////////////////////测速/////////////
             real_speed = gpt12_get(GPT12_T2);
                          gpt12_clear(GPT12_T2);  //清零

   // real_speed = limit(real_speed,2000); //限幅保护   
              //last_speed=real_speed; 
               
    distance+=real_speed*0.0105;    //测距离  化为厘米为单位  0.0085
}           

/*********************速度PI*********************/

static float SpeedErrorInteg = 0;       //偏差积分 
float I_Calculate=0;//积分I计算值
float P_Calculate=0;//比例P计算值
static int8 separate=1;    //积分分离系数
float Speed_P_I_Control(float *PID, int16 speed, int16 set_speed)//因为角度变化小，需要是浮点型
{   
    float  SpeedControlOut;//最终计算量
    int16 SpeedError;   // 当前误差
   
    static uint8 k;
    static float PreError[10]={0};   
    SpeedError = set_speed - speed;  //计算速度偏差
    SpeedErrorInteg=0;          //误差积分
    for(k=9;k>0;k--)          //20次误差（--比++快）
    {  
     PreError[k]=PreError[k-1];
     SpeedErrorInteg+=PreError[k];
    }
     PreError[0]=SpeedError;
     SpeedErrorInteg+=PreError[0]; //积分项
    
     I_Calculate = separate * PID[KI] * SpeedErrorInteg;  //积分I计算值
  
     P_Calculate=PID[KP]*SpeedError;//比例P计算值

         if(SpeedError<0)//超速，
         {
            SpeedControlOut=0;
             /// SpeedControlOut=-5;

         }
         else
         {
      SpeedControlOut = P_Calculate+I_Calculate;  // 最终输出 
         }
     return   SpeedControlOut;
     }

/******************** 电机初始化***************
函数：void init_PWM(void)
参数：  无
说明：  分母50000，使用ATOM0，如需修改引脚修改对应宏定义即可
                gtm_pwm_init(ATOM0_CH4_P02_4, 50,    0);//ATOM 0模块的通道4 使用P02_4引脚输出PWM  PWM频率50HZ
                                占空比百分之0/GTM_ATOM0_PWM_DUTY_MAX*100  GTM_ATOM0_PWM_DUTY_MAX宏定义在zf_gtm_pwm.h
             //每个通道都可以输出不同频率的PWM
返回值：无
日期：2021-1-13    00:15
修改时间：2021-1-13    00:15
作者： 混子    */

/****************************电机输出**********************
函数：void go_motor (int16 left_PWM,int16 right_PWM)
参数：  int16 left_PWM,int16 right_PWM
说明：pwm_duty(ATOM0_CH4_P02_4, 5000);//设置占空比为百分之5000/GTM_ATOM0_PWM_DUTY_MAX*100
返回值：无
日期：2021-1-13    00:15
修改时间：2021-1-13    00:15
作者： 混子    */
#define Duty_Max  6000   //限幅最大值

void go_motor (int16 PWM_Duty1,int16 PWM_Duty2)
{
    if(0<=PWM_Duty1) //电机1   正转 设置占空比为 百分之 (1000/GTM_ATOM0_PWM_DUTY_MAX*100)
           {
               pwm_duty(MOTOR1_PWM, PWM_Duty1);
               gpio_set(MOTOR1_DIR, 0);
           }
           else                //电机1   反转
           {
               pwm_duty(MOTOR1_PWM, -PWM_Duty1);
               gpio_set(MOTOR1_DIR, 1);
           }
    if(0<=PWM_Duty2) //电机2   正转
         {
             pwm_duty(MOTOR2_PWM, PWM_Duty2);
             gpio_set(MOTOR2_DIR, 0);
         }
         else                //电机2   反转
         {
             pwm_duty(MOTOR2_PWM, -PWM_Duty2);
             gpio_set(MOTOR2_DIR, 1);
         }



}


    struct _pidd{
            float ActVal;
                float errr ;
                float  Out_add;
                float  OutVal ;
                float err_last2;
                float err_lastt ;
                float SetVal;
                float kpp;
                float kdd;
                float kii;
    }   pDelta_PIDStructrre;
    struct _pidcsd{
              float ActVal;
                  float errr ;
                  float  Out_add;
                  float  OutVal ;
                  float err_last2;
                  float err_lastt ;
                  float SetVal;
                  float kpp;
                  float kdd;
                  float kii;
      }   CS_RR;
      struct _pidcslld{
                float ActVal;
                    float errr ;
                    float  Out_add;
                    float  OutVal ;
                    float err_last2;
                    float err_lastt ;
                    float SetVal;
                    float kpp;
                    float kdd;
                    float kii;
        }   CS_LL;

    struct _hdpidd{
              float ActVal;
                  float errr ;
                  float  Out_add;
                  float  OutVal ;
                  float err_last2;
                  float err_lastt ;
                  float SetVal;
                  float kpp;
                  float kdd;
                  float kii;
      }PIDStructrre;


    void DJPID_init()
    {
        pDelta_PIDStructrre.ActVal=0;
        pDelta_PIDStructrre.errr =0;
        pDelta_PIDStructrre.Out_add=0;
        pDelta_PIDStructrre.OutVal =0;
        pDelta_PIDStructrre.err_last2=0;
        pDelta_PIDStructrre.err_lastt =0;
      //  pDelta_PIDStructrre.kpp=6;
      // pDelta_PIDStructrre.kdd=4;
    //    pDelta_PIDStructrre.kii=6.5;
    }

    void Delta_PID(float Actuall,int type)
        {
            pDelta_PIDStructrre.SetVal=type;
           pDelta_PIDStructrre.ActVal  = Actuall;
           pDelta_PIDStructrre.errr     = pDelta_PIDStructrre.SetVal -pDelta_PIDStructrre.ActVal;
           pDelta_PIDStructrre.Out_add = 19* (pDelta_PIDStructrre.errr-pDelta_PIDStructrre.err_lastt)
                                        + 4.89* (pDelta_PIDStructrre.errr);


           pDelta_PIDStructrre.OutVal   += (int16)pDelta_PIDStructrre.Out_add;
           pDelta_PIDStructrre.err_last2 =pDelta_PIDStructrre.err_lastt;
           pDelta_PIDStructrre.err_lastt =pDelta_PIDStructrre.errr;

//            if(pDelta_PIDStructrre.OutVal > sp_max)
//                {pDelta_PIDStructrre.OutVal =sp_max;
//                }
//            if(pDelta_PIDStructrre.OutVal < sp_min)
//            {
//               pDelta_PIDStructrre.OutVal = sp_min;
//
//            }
           pDelta_PIDStructrre.OutVal=((pDelta_PIDStructrre.OutVal>sp_max) ? sp_max :pDelta_PIDStructrre.OutVal);
           pDelta_PIDStructrre.OutVal=((pDelta_PIDStructrre.OutVal<sp_min) ? sp_min :pDelta_PIDStructrre.OutVal);

            if( pDelta_PIDStructrre.OutVal >=0)
               {
                   pwm_duty( ATOM1_CH7_P02_7, pDelta_PIDStructrre.OutVal );
                   pwm_duty(ATOM1_CH6_P02_6, 0);
                   pwm_duty( ATOM1_CH4_P02_4,pDelta_PIDStructrre.OutVal );
                   pwm_duty( ATOM1_CH5_P02_5,0);
               }
               if( pDelta_PIDStructrre.OutVal <0)
                 {
                     pwm_duty( ATOM1_CH6_P02_6,- pDelta_PIDStructrre.OutVal );
                     pwm_duty(ATOM1_CH7_P02_7,0);
                     pwm_duty( ATOM1_CH5_P02_5,- pDelta_PIDStructrre.OutVal );
                                         pwm_duty(ATOM1_CH4_P02_4,0);
                 }

        }


    uint8 Threshold_deal(uint8* image,
                         uint16 col,
                         uint16 row,
                         uint32 pixel_threshold) {
    #define GrayScale 256
      uint16 width = col;
      uint16 height = row;
      int pixelCount[GrayScale];
      float pixelPro[GrayScale];
      int i, j, pixelSum = width * height;
      uint8 threshold = 0;
      uint8* data = image;  //指向像素数据的指针
      for (i = 0; i < GrayScale; i++) {
        pixelCount[i] = 0;
        pixelPro[i] = 0;
      }

      uint32 gray_sum = 0;
      //统计灰度级中每个像素在整幅图像中的个数
      for (i = 0; i < height; i += 1) {
        for (j = 0; j < width; j += 1) {
          // if((sun_mode&&data[i*width+j]<pixel_threshold)||(!sun_mode))
          //{
          pixelCount[(
              int)data[i * width + j]]++;  //将当前的点的像素值作为计数数组的下标
          gray_sum += (int)data[i * width + j];  //灰度值总和
          //}
        }
      }

      //计算每个像素值的点在整幅图像中的比例

      for (i = 0; i < GrayScale; i++) {
        pixelPro[i] = (float)pixelCount[i] / pixelSum;
      }

      //遍历灰度级[0,255]
      float w0, w1, u0tmp, u1tmp, u0, u1, u, deltaTmp, deltaMax = 0;

      w0 = w1 = u0tmp = u1tmp = u0 = u1 = u = deltaTmp = 0;
      for (j = 0; j < pixel_threshold; j++) {
        w0 +=
            pixelPro[j];  //背景部分每个灰度值的像素点所占比例之和 即背景部分的比例
        u0tmp += j * pixelPro[j];  //背景部分 每个灰度值的点的比例 *灰度值

        w1 = 1 - w0;
        u1tmp = gray_sum / pixelSum - u0tmp;

        u0 = u0tmp / w0;    //背景平均灰度
        u1 = u1tmp / w1;    //前景平均灰度
        u = u0tmp + u1tmp;  //全局平均灰度
        deltaTmp = w0 * pow((u0 - u), 2) + w1 * pow((u1 - u), 2);
        if (deltaTmp > deltaMax) {
          deltaMax = deltaTmp;
          threshold = j;
        }
        if (deltaTmp < deltaMax) {
          break;
        }
      }
      return threshold;
    }

void sengfl(float m1,float m2)
{
    unsigned char tail[4]={0x00,0x00,0x80,0x7f};
    float fdata[2];
    fdata[0]=m1;
    fdata[1]=m2;
    seekfree_wireless_send_buff((char*)fdata,sizeof(float)*2);
    seekfree_wireless_send_buff(tail,4);
}
uint8 otsuThreshold(uint8 *image, uint16 col, uint16 row)   //大津法二值化求阈值  需优化
{
    #define GrayScale 256
    uint16 Image_Width  = col;
    uint16 Image_Height = row;
    int X; uint16 Y;
    uint8* data = image;
    int HistGram[GrayScale];

    for (Y = 0; Y < GrayScale; Y++)
    {
        HistGram[Y] = 0; //初始化灰度直方图
    }
    for (Y = 0; Y <Image_Height; Y+=2) //Y<Image_Height改为Y =Image_Height；以便进行 行二值化
    {
        //Y=Image_Height;
        for (X = 0; X < Image_Width; X+=2)
        {
        HistGram[(int)data[Y*Image_Width + X]]++; //统计每个灰度值的个数信息
        }
    }
    uint32 Amount = 0;
    uint32 PixelBack = 0;
    uint32 PixelIntegralBack = 0;
    uint32 PixelIntegral = 0;
    int32 PixelIntegralFore = 0;
    int32 PixelFore = 0;
    double OmegaBack=0, OmegaFore=0, MicroBack=0, MicroFore=0, SigmaB=0, Sigma=0; // 类间方差;
    int16 MinValue=0, MaxValue=0;
    uint8 Threshold = 0;
    for (MinValue = 0; MinValue < 256 && HistGram[MinValue] == 0; MinValue++) ;        //获取最小灰度的值
    for (MaxValue = 255; MaxValue > MinValue && HistGram[MinValue] == 0; MaxValue--) ; //获取最大灰度的值

    if (MaxValue == MinValue)
    {
        return MaxValue;          // 图像中只有一个颜色
    }
    if (MinValue + 1 == MaxValue)
    {
        return MinValue;      // 图像中只有二个颜色
    }

    for (Y = MinValue; Y <= MaxValue; Y++)
    {
        Amount += HistGram[Y];        //  像素总数
    }

    PixelIntegral = 0;
    for (Y = MinValue; Y <= MaxValue; Y++)
    {
        PixelIntegral += HistGram[Y] * Y;//灰度值总数
    }
    SigmaB = -1;
    for (Y = MinValue; Y < MaxValue; Y++)
    {
          PixelBack = PixelBack + HistGram[Y];    //前景像素点数
          PixelFore = Amount - PixelBack;         //背景像素点数
          OmegaBack = (double)PixelBack / Amount;//前景像素百分比
          OmegaFore = (double)PixelFore / Amount;//背景像素百分比
          PixelIntegralBack += HistGram[Y] * Y;  //前景灰度值
          PixelIntegralFore = PixelIntegral - PixelIntegralBack;//背景灰度值
          MicroBack = (double)PixelIntegralBack / PixelBack;//前景灰度百分比
          MicroFore = (double)PixelIntegralFore / PixelFore;//背景灰度百分比
          Sigma = OmegaBack * OmegaFore * (MicroBack - MicroFore) * (MicroBack - MicroFore);//g
          if (Sigma > SigmaB)//遍历最大的类间方差g
          {
              SigmaB = Sigma;
              Threshold = Y;
          }
    }
   return Threshold;
}




//  滤波噪点
void Bin_Image_Filter (void)
{
    sint16 nr; //行
    sint16 nc; //列

    for (nr = 1; nr < MT9V032_H - 1; nr++)
    {
        for (nc = 1; nc < MT9V032_W - 1; nc = nc + 1)
        {
            if ((image_data[nr][nc] == 0)
                    && (image_data[nr - 1][nc] + image_data[nr + 1][nc] + image_data[nr][nc + 1] + image_data[nr][nc - 1] > 2))
            {
                image_data[nr][nc] = 1;
            }
            else if ((image_data[nr][nc] == 1)
                    && (image_data[nr - 1][nc] + image_data[nr + 1][nc] + image_data[nr][nc + 1] + image_data[nr][nc - 1] < 2))
            {
                image_data[nr][nc] = 0;
            }
        }
    }
}
void HDDJPID_init()
  {
     PIDStructrre.ActVal=0;
     PIDStructrre.errr =0;
     PIDStructrre.Out_add=0;
     PIDStructrre.OutVal =0;
     PIDStructrre.err_last2=0;
     PIDStructrre.err_lastt =0;
  }
  void HDDelta_PID(float Actuall,int type)
  {
     PIDStructrre.SetVal=type;
    PIDStructrre.ActVal  = Actuall;
    PIDStructrre.errr     =PIDStructrre.SetVal -PIDStructrre.ActVal;
    PIDStructrre.Out_add = 4 * (PIDStructrre.errr-PIDStructrre.err_lastt)
                                  + 7 * (PIDStructrre.errr);
    PIDStructrre.OutVal   += (int16)PIDStructrre.Out_add;
    PIDStructrre.err_last2 =PIDStructrre.err_lastt;
    PIDStructrre.err_lastt =PIDStructrre.errr;
      if(PIDStructrre.OutVal > hdsp_max)
          {PIDStructrre.OutVal =hdsp_max;
          }
      if(PIDStructrre.OutVal < hdsp_min)
      {
        PIDStructrre.OutVal = hdsp_min;
      }
      if( PIDStructrre.OutVal >0)
         {
             pwm_duty( ATOM1_CH7_P02_7, PIDStructrre.OutVal );
             pwm_duty(ATOM1_CH6_P02_6, 0);
         }
         if( PIDStructrre.OutVal <0)
           {
               pwm_duty( ATOM1_CH6_P02_6,0);
               pwm_duty(ATOM1_CH7_P02_7,0);
           }
  }


  void CSRDJPID_init()
    {
        CS_RR.ActVal=0;
        CS_RR.errr =0;
        CS_RR.Out_add=0;
        CS_RR.OutVal =0;
        CS_RR.err_last2=0;
        CS_RR.err_lastt =0;
      //  CS_RR.kpp=6;
      // CS_RR.kdd=4;
    //    CS_RR.kii=6.5;
    }

    void CSRDelta_PID(float Actuall,int type)
        {
            CS_RR.SetVal=type;
           CS_RR.ActVal  = Actuall;
           CS_RR.errr     = CS_RR.SetVal -CS_RR.ActVal;
           CS_RR.Out_add = (50) * (CS_RR.errr-CS_RR.err_lastt)
                                        + (2.5)* (CS_RR.errr);


           CS_RR.OutVal   += (int16)CS_RR.Out_add;
           CS_RR.err_last2 =CS_RR.err_lastt;
           CS_RR.err_lastt =CS_RR.errr;

//            if(CS_RR.OutVal > sp_max)
//                {CS_RR.OutVal =sp_max;
//                }
//            if(CS_RR.OutVal < sp_min)
//            {
//               CS_RR.OutVal = sp_min;
//
//            }
           CS_RR.OutVal=((CS_RR.OutVal>sp_max) ? sp_max :CS_RR.OutVal);
           CS_RR.OutVal=((CS_RR.OutVal<sp_min) ? sp_min :CS_RR.OutVal);

            if( CS_RR.OutVal >=0)
               {
//                   pwm_duty( ATOM1_CH7_P02_7, CS_RR.OutVal );
//                   pwm_duty(ATOM1_CH6_P02_6, 0);
                   pwm_duty( ATOM1_CH4_P02_4,CS_RR.OutVal );
                   pwm_duty( ATOM1_CH5_P02_5,0);
               }
               if( CS_RR.OutVal <0)
                 {
//                     pwm_duty( ATOM1_CH6_P02_6,- CS_RR.OutVal );
//                     pwm_duty(ATOM1_CH7_P02_7,0);
                     pwm_duty( ATOM1_CH5_P02_5,- CS_RR.OutVal );
                                         pwm_duty(ATOM1_CH4_P02_4,0);
                 }

        }

    void CSLDJPID_init()
       {
           CS_LL.ActVal=0;
           CS_LL.errr =0;
           CS_LL.Out_add=0;
           CS_LL.OutVal =0;
           CS_LL.err_last2=0;
           CS_LL.err_lastt =0;
         //  CS_LL.kpp=6;
         // CS_LL.kdd=4;
       //    CS_LL.kii=6.5;
       }

       void CSLDelta_PID(float Actuall,int type)
           {
               CS_LL.SetVal=type;
              CS_LL.ActVal  = Actuall;
              CS_LL.errr     = CS_LL.SetVal -CS_LL.ActVal;
              CS_LL.Out_add = (50) * (CS_LL.errr-CS_LL.err_lastt)
                                           + (2.5)* (CS_LL.errr);


              CS_LL.OutVal   += (int16)CS_LL.Out_add;
              CS_LL.err_last2 =CS_LL.err_lastt;
              CS_LL.err_lastt =CS_LL.errr;

   //            if(CS_LL.OutVal > sp_max)
   //                {CS_LL.OutVal =sp_max;
   //                }
   //            if(CS_LL.OutVal < sp_min)
   //            {
   //               CS_LL.OutVal = sp_min;
   //
   //            }
              CS_LL.OutVal=((CS_LL.OutVal>sp_max) ? sp_max :CS_LL.OutVal);
              CS_LL.OutVal=((CS_LL.OutVal<sp_min) ? sp_min :CS_LL.OutVal);

               if( CS_LL.OutVal >=0)
                  {
                      pwm_duty( ATOM1_CH7_P02_7, CS_LL.OutVal );
                      pwm_duty(ATOM1_CH6_P02_6, 0);
//                      pwm_duty( ATOM1_CH4_P02_4,CS_LL.OutVal );
//                      pwm_duty( ATOM1_CH5_P02_5,0);
                  }
                  if( CS_LL.OutVal <0)
                    {
                        pwm_duty( ATOM1_CH6_P02_6,- CS_LL.OutVal );
                        pwm_duty(ATOM1_CH7_P02_7,0);
//                        pwm_duty( ATOM1_CH5_P02_5,- CS_LL.OutVal );
//                                            pwm_duty(ATOM1_CH4_P02_4,0);
                    }

           }
  void CS_control(int16 realdj,int16 High_Speed )
  {
//      angle = realdj;//以PWM差的形式表示打角
             /****************************************************************************/
          /****************************     阿克曼加减差速      ****************************/
          /****************************************************************************/

           if(realdj>0)   //右转
          {
             a=(int16)(42*(realdj)*1.0/(766 - 673)) ;   //angle_max   40/
             if(a>42)  a=42;
             if(a<0)   a=0;
             Temp_Orr = tan((a*3.14)/180) * 13.4 / 40;          //15.4
             Left_Speed =  0.05* High_Speed * (0.70 + circle_out*Temp_Orr); //第一个常数可以加大差速，第二个常数可以提前差速 //原来是0.78
             Right_Speed =  0.05* High_Speed * (0.70 - circle_in*Temp_Orr);//原来是1
//             Left_Speed=(Left_Speed>=20) ? 20:Left_Speed;
//                       Left_Speed=(Left_Speed<=-20) ? -20:Left_Speed;
//                       Right_Speed=(Right_Speed>=20) ? 20:Right_Speed;
//                       Right_Speed=(Right_Speed<=-20) ? -20:Right_Speed;
             Left_High_Speed = Left_Speed;
             Right_High_Speed = Right_Speed;
          }
          else
          {
              a=(int16)(42*(-realdj)*1.0/(673 - 570)) ;  //angle_max   40
              if(a>42)  a=42;
              if(a<0)   a=0;

              Temp_Orr = tan((a*3.14)/180) * 13.4 / 40;
              Left_Speed =  0.05* High_Speed * (0.70 - circle_in*Temp_Orr); //原来是1
              Right_Speed =  0.05* High_Speed * (0.70 +circle_out*Temp_Orr);//原来是0.78
//              Left_Speed=(Left_Speed>=20) ? 20:Left_Speed;
//              Left_Speed=(Left_Speed<=-20) ? -20:Left_Speed;
//              Right_Speed=(Right_Speed>=20) ? 20:Right_Speed;
//              Right_Speed=(Right_Speed<=-20) ? -20:Right_Speed;
              Left_High_Speed = Left_Speed;
              Right_High_Speed = Right_Speed;
          }



  }
